<Tabs defaultValue="css-vars" className="w-full">
  <TabsList className="grid w-full grid-cols-2">
    <TabsTrigger value="css-vars">CSS Variables</TabsTrigger>
    <TabsTrigger value="util">Utility classes</TabsTrigger>
  </TabsList>
  <TabsContent value="css-vars">
    ```tsx file=<rootDir>/src/components/css-vars/progress.tsx title="components/ui/progress.tsx"
    ```
  </TabsContent>
  <TabsContent value="util">
    ```tsx file=<rootDir>/src/components/ui/progress.tsx title="components/ui/progress.tsx"
    ```
  </TabsContent>
</Tabs>

<br />

## Usage

```ts
import * as React from 'react'

import { Progress } from '@/components/ui/progress'
```

<br />

```tsx
const [progress, setProgress] = React.useState(13);

React.useEffect(() => {
  const timer = setTimeout(() => setProgress(66), 500);
  return () => clearTimeout(timer);
}, [])
```

<br />

```tsx
<Progress value={progress} className="w-[60%]" />
```