## Installation

### via Shadcn cli

<Tabs defaultValue="css-vars" className="w-full">
  <TabsList className="grid w-full grid-cols-2">
    <TabsTrigger value="css-vars">CSS Variables</TabsTrigger>
    <TabsTrigger value="util">Utility classes</TabsTrigger>
  </TabsList>
  <TabsContent value="css-vars">
    <Tabs defaultValue="pnpm" className="w-full">
      <TabsList className="grid w-full grid-cols-4">
        <TabsTrigger value="pnpm">pnpm</TabsTrigger>
        <TabsTrigger value="npm">npm</TabsTrigger>
        <TabsTrigger value="yarn">yarn</TabsTrigger>
        <TabsTrigger value="bun">bun</TabsTrigger>
      </TabsList>
      <TabsContent value="pnpm">
      ```
      pnpm dlx shadcn@latest add https://neobrutalism.dev/r/hover-card.json
      ```
      </TabsContent>
      <TabsContent value="npm">
      ```
      npx shadcn@latest add https://neobrutalism.dev/r/hover-card.json
      ```
      </TabsContent>
      <TabsContent value="yarn">
      ```
      npx shadcn@latest add https://neobrutalism.dev/r/hover-card.json
      ```
      </TabsContent>
      <TabsContent value="bun">
      ```
      bunx --bun shadcn@latest add https://neobrutalism.dev/r/hover-card.json
      ```
      </TabsContent>
    </Tabs>
  </TabsContent>
  <TabsContent value="util">
    <Tabs defaultValue="pnpm" className="w-full">
      <TabsList className="grid w-full grid-cols-4">
        <TabsTrigger value="pnpm">pnpm</TabsTrigger>
        <TabsTrigger value="npm">npm</TabsTrigger>
        <TabsTrigger value="yarn">yarn</TabsTrigger>
        <TabsTrigger value="bun">bun</TabsTrigger>
      </TabsList>
      <TabsContent value="pnpm">
      ```
      pnpm dlx shadcn@latest add http://util.neobrutalism.dev/r/hover-card.json
      ```
      </TabsContent>
      <TabsContent value="npm">
      ```
      npx shadcn@latest add http://util.neobrutalism.dev/r/hover-card.json
      ```
      </TabsContent>
      <TabsContent value="yarn">
      ```
      npx shadcn@latest add http://util.neobrutalism.dev/r/hover-card.json
      ```
      </TabsContent>
      <TabsContent value="bun">
      ```
      bunx --bun shadcn@latest add http://util.neobrutalism.dev/r/hover-card.json
      ```
      </TabsContent>
    </Tabs>
  </TabsContent>
</Tabs>

<br />

### Manually

<Tabs defaultValue="css-vars" className="w-full">
  <TabsList className="grid w-full grid-cols-2">
    <TabsTrigger value="css-vars">CSS Variables</TabsTrigger>
    <TabsTrigger value="util">Utility classes</TabsTrigger>
  </TabsList>
  <TabsContent value="css-vars">
    ```tsx file=<rootDir>/src/components/css-vars/hover-card.tsx title="components/ui/hover-card.tsx"
    ```
  </TabsContent>
  <TabsContent value="util">
    ```tsx file=<rootDir>/src/components/ui/hover-card.tsx title="components/ui/hover-card.tsx"
    ```
  </TabsContent>
</Tabs>

<br />

## Usage

```ts
import { Button } from '@/components/ui/button'
import {
  HoverCard,
  HoverCardContent,
  HoverCardTrigger,
} from '@/components/ui/hover-card'
```

<br />

```tsx
<HoverCard>
  <HoverCardTrigger asChild>
    <Button variant="noShadow">Hover</Button>
  </HoverCardTrigger>
  <HoverCardContent>
    The React Framework – created and maintained by @vercel.
  </HoverCardContent>
</HoverCard>
```