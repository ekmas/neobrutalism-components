---
title: Command
description: Fast, composable, unstyled command menu for React.
shadcnDocsLink: https://ui.shadcn.com/docs/components/command
---

<ComponentPreview component="command">
  ```tsx file=<rootDir>/src/examples/ui/command/index.tsx
  ```
</ComponentPreview>

## Installation

<Installation component="command">
  ```tsx file=<rootDir>/src/components/ui/command.tsx
  ```
</Installation>

## Usage

```ts
import {
  Calculator,
  Calendar,
  CreditCard,
  Settings,
  Smile,
  User,
} from "lucide-react"

import * as React from "react"

import {
  CommandDialog,
  CommandEmpty,
  CommandGroup,
  CommandInput,
  CommandItem,
  CommandList,
  CommandSeparator,
  CommandShortcut,
} from "@/components/ui/command"
```

```tsx
const [open, setOpen] = React.useState(false)

React.useEffect(() => {
  const down = (e: KeyboardEvent) => {
    if (e.key === "j" && (e.metaKey || e.ctrlKey)) {
      e.preventDefault()
      setOpen((open) => !open)
    }
  }

  document.addEventListener("keydown", down)
  return () => document.removeEventListener("keydown", down)
}, [])
```

```tsx
<>
  <p className="text-foreground text-sm">
    Press{" "}
    <kbd className="bg-main text-main-foreground pointer-events-none inline-flex h-5 items-center gap-1 rounded-base border-2 px-1.5 font-mono text-[10px] font-heading select-none">
      <span className="text-xs">⌘</span>J
    </kbd>
  </p>
  <CommandDialog open={open} onOpenChange={setOpen}>
    <CommandInput placeholder="Type a command or search..." />
    <CommandList>
      <CommandEmpty>No results found.</CommandEmpty>
      <CommandGroup heading="Suggestions">
        <CommandItem>
          <Calendar />
          <span>Calendar</span>
        </CommandItem>
        <CommandItem>
          <Smile />
          <span>Search Emoji</span>
        </CommandItem>
        <CommandItem>
          <Calculator />
          <span>Calculator</span>
        </CommandItem>
      </CommandGroup>
      <CommandSeparator />
      <CommandGroup heading="Settings">
        <CommandItem>
          <User />
          <span>Profile</span>
          <CommandShortcut>⌘P</CommandShortcut>
        </CommandItem>
        <CommandItem>
          <CreditCard />
          <span>Billing</span>
          <CommandShortcut>⌘B</CommandShortcut>
        </CommandItem>
        <CommandItem>
          <Settings />
          <span>Settings</span>
          <CommandShortcut>⌘S</CommandShortcut>
        </CommandItem>
      </CommandGroup>
    </CommandList>
  </CommandDialog>
</>
```

## Examples

### Default

<ComponentPreview component="command" example="default">
  ```tsx file=<rootDir>/src/examples/ui/command/index.tsx
  ```
</ComponentPreview>

### Basic

A simple command menu in a dialog. `CommandDialog` already wraps its children in a `Command`, so you only need the input and the list.

<ComponentPreview component="command" example="basic">
  ```tsx file=<rootDir>/src/examples/ui/command/basic.tsx
  ```
</ComponentPreview>

### Shortcuts

Use `CommandShortcut` to show keyboard hints next to an item.

<ComponentPreview component="command" example="shortcuts">
  ```tsx file=<rootDir>/src/examples/ui/command/shortcuts.tsx
  ```
</ComponentPreview>

### Groups

A command menu with groups, icons and separators.

<ComponentPreview component="command" example="groups">
  ```tsx file=<rootDir>/src/examples/ui/command/groups.tsx
  ```
</ComponentPreview>

### Scrollable

Scrollable command menu with multiple items.

<ComponentPreview component="command" example="scrollable">
  ```tsx file=<rootDir>/src/examples/ui/command/scrollable.tsx
  ```
</ComponentPreview>
